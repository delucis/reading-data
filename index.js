/**
 * @module reading-data
 * @requires winston
 */

const log = require('winston')

const ReadingData = (function () {
  // PRIVATE VARIABLES
  return {
    // PUBLIC VARIABLES
  }
}())

module.exports = ReadingData
